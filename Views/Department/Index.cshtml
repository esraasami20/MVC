
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
    <link href="~/Content/bootstrap.css" rel="stylesheet" />
</head>
<body>
    <div class="container text-center py-5">
        <a href="/Department/addDept" class="btn btn-primary">New Department</a>
        <table class="table table-bordered">
            <thead>
                <tr>
                    <td>ID</td>
                    <td>Name</td>
                    <td>Operation</td>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.ID</td>
                        <td>@item.Name</td>
                        <td>
                            <a href="/Department/Update/@item.ID" class="btn btn-dark">Edit Department</a> |
                            <a href="/Department/Details/@item.ID" class="btn btn-warning">Department Details</a> |
                            <a href="/Department/Delete/@item.ID" class="btn btn-danger">Delete Department</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</body>
</html>
